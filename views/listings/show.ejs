<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= listing.owner.username %>'s listing</title>
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>

    <h1>A listing in <%= listing.city %></h1>

    <h2>Details</h2>
    <p>House Size: <%= listing.size %></p>
    <p>Price: $<%= listing.price %></p>
    <p>Street Address: <%= listing.streetAddress %></p>

    <h2>Owner</h2>
    <% if(listing.owner._id.equals(user._id)) { %>
    <p>You own this!</p>
    <a href="/listings/<%= listing._id %>/edit">Edit this property</a>
    <form action="/listings/<%= listing._id %>?_method=DELETE" method="POST">
      <button type="submit">Delete this property</button>
    </form>
    <% } else { %>
    <p>You down own it, <%= listing.owner.username %> does!</p>
    <% }%>

    <h2>Favorites</h2>
    <p>Favorited by <%= listing.favoritedByUsers.length %></p>
    <% if(userHasLikedItem) { %>
    <p>You have liked this item</p>
        <form
      action="/listings/<%= listing._id %>/favorited-by/<%= user._id %>?_method='DELETE'"
      method="POST"
    >
      <button type="submit">Unfavorite this Property</button>
    </form>

    <% } else { %>
    <form
      action="/listings/<%= listing._id %>/favorited-by/<%= user._id %>"
      method="POST"
    >
      <button type="submit">Favorite this Property</button>
    </form>
    <% } %>
  </body>
</html>